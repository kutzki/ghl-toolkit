{
  "_instructions": "Merge the mcpServers block into ~/.config/gemini/settings.json (Mac/Linux) or %USERPROFILE%/.config/gemini/settings.json (Windows). Create the file if it doesn't exist.",
  "mcpServers": {
    "aio-ghl-mcp": {
      "command": "node",
      "args": ["/ABSOLUTE/PATH/TO/ghl-toolkit/dist/server.js"],
      "env": {
        "GHL_API_KEY": "your_private_integrations_api_key",
        "GHL_BASE_URL": "https://services.leadconnectorhq.com",
        "GHL_LOCATION_ID": "your_location_id"
      }
    }
  }
}
